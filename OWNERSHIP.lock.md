# OWNERSHIP LOCK — не конфліктувати з іншим ШІ

**Статус:** ACTIVE  
**З:** 2026-08-03  
**Власник зараз:** інший ШІ (підключає Boss)  
**Atlas (Cursor):** HANDS OFF

## Що це означає

1. **Не запускати** бота локально (`python main.py` / polling) — один токен = один процес.
2. **Не ставити webhook** і не чіпати Telegram API з токенами ботів.
3. **Не деплоїти / не пушити** в Railway / GitHub `Paraprob/angarta-bot`, поки lock активний.
4. **Не редагувати** файли в:
   - `C:\Users\dmitr\Desktop\angarta-bot`
   - `D:\Claude\angarta-bot`
   - будь-яку нову копію цього репо, з якою працює інший ШІ
5. **Не міняти** `.env` / токени / Notion vars для цих ботів.

## Токени під lock (не використовувати в runtime)

- `@anghartainfobot` — production-кандидат (Приймальня Ангарта) — **основний код у репо**
- `@coAtlas_bot` — окремий бот; теж не чіпати без явної команди Boss

## Як зняти lock

Boss пише явно, напр.:  
«Atlas, забирай бота назад» / «зніми lock з angarta-bot».

Доти — лише консультації текстом, без запуску й без правок коду бота.
